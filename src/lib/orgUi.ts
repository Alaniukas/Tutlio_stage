/** Org-admin tutor checkbox filter lists — ~3 rows visible, scroll for more. */
export const ORG_TUTOR_FILTER_SCROLL_CLASS = 'max-h-28 overflow-y-auto';

/** Org-admin tutor Select dropdowns (incl. sticky search) — ~3 options visible. */
export const ORG_TUTOR_SELECT_SCROLL_CLASS = 'max-h-40 overflow-y-auto';

/** Org-admin tutor pickers with taller rows (e.g. invoice cards). */
export const ORG_TUTOR_CARD_LIST_SCROLL_CLASS = 'max-h-48 overflow-y-auto';
